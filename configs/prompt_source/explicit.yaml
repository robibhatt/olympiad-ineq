# Explicit prompt source configuration
# Provides a list of prompt items directly in the config

_target_: src.data_gen.prompt_source.ExplicitPromptSource

items:
  - id: "example_1"
    messages:
      - role: "system"
        content: "You are a helpful assistant."
      - role: "user"
        content: "Hello, how are you?"
  - id: "example_2"
    messages:
      - role: "user"
        content: "What is 2+2?"
